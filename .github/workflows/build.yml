name: Build Image

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build and Push
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Image
        uses: ublue-os/bluebuild@v2
        with:
          containerfile: Containerfile
