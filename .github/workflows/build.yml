name: Build Image

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build and Push
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Image
        uses: ublue-os/build-image-action@main
        with:
          containerfile: Containerfile
